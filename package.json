{
  "name": "geo-admin",
  "version": "0.1.0",
  "private": true,
  "description": "GEO Platform Admin Monorepo",
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm --filter web build",
    "preview": "pnpm --filter web preview",
    "lint": "pnpm -r lint",
    "format": "prettier --write .",
    "dev:web": "pnpm --filter web dev",
    "dev:api": "pnpm --filter @geo-admin/api dev",
    "build:web": "pnpm --filter web build"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "packageManager": "pnpm@9.12.0",
  "devDependencies": {
    "@types/node": "^20.14.0",
    "prettier": "^3.3.0",
    "typescript": "^5.5.0"
  }
}