{
  "name": "geo-admin",
  "version": "0.1.0",
  "private": true,
  "description": "GEO Platform Admin Monorepo",
  "scripts": {
    "lint": "pnpm -r lint",
    "format": "prettier --write .",
    "dev:api": "pnpm --filter @geo-admin/api dev",
    "dev:site": "pnpm --filter @geo-admin/gen-site dev",
    "build:site": "pnpm --filter @geo-admin/gen-site build",
    "preview:site": "pnpm --filter @geo-admin/gen-site preview"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "packageManager": "pnpm@9.12.0",
  "devDependencies": {
    "@types/node": "^20.14.0",
    "prettier": "^3.3.0",
    "typescript": "^5.5.0"
  }
}