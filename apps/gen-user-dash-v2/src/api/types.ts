/**
 * 接口契约类型（已收口到共享包 @geo-admin/contracts）
 * -------------------------------------------------
 * 本文件只做重导出，避免破坏既有 `@/api/types` 导入路径。
 * 新增/修改契约请改 packages/contracts/src/{api,enums,entities,schemas}.ts。
 */
export * from '@geo-admin/contracts';
